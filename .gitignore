# 依赖与构建
node_modules/
dist/

# 环境变量（含 publishable key 与环境 ID，不入库；模板见 .env.example）
.env
.env.local
.env.*.local

# 密钥文件（云函数密钥一律走环境变量，禁止落盘）
*.pem
*.key
tcb_custom_login.json

# 临时产物
*.local
.DS_Store
_*.png
docs/_*.png
cloudbase-lint.mjs
